# project/data

Datasets, exports, and research dumps for this project. Register everything you
put here in `../DATA-LOG.md` (source, date, sensitivity).

**Do not commit secrets, credentials, API keys, or PII.** Keep large or sensitive
raw data out of version control if needed (add to `.gitignore`).
