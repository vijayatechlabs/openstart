# Stack & Commands

The tech stack and the exact commands that implement the **Definition of Done**
ritual (see `.ai/framework/AGENT-GUIDE.md` §9). Agents read this file to know how
to build, test, and check *this specific* project — the ritual is standard, these
commands are not.

Fill these in when the stack is set up. Leave a cell blank only if it genuinely
doesn't apply; use `TODO:` if it's undecided.

## Stack
- **Language(s):** <!-- e.g. TypeScript, Python -->
- **Framework(s):** <!-- e.g. Next.js 15, FastAPI -->
- **Package manager:** <!-- e.g. pnpm, uv -->
- **Runtime / version:** <!-- e.g. Node 22, Python 3.12 -->
- **Database / services:** <!-- e.g. Postgres, Supabase -->
- **Hosting / deploy target:** <!-- e.g. Vercel, Cloudflare, VPS -->

## Commands
| Purpose | Command |
|---|---|
| Install deps | <!-- e.g. pnpm install --> |
| Dev server | <!-- e.g. pnpm dev --> |
| Build | <!-- e.g. pnpm build --> |
| Test | <!-- e.g. pnpm test --> |
| Lint | <!-- e.g. pnpm lint --> |
| Type-check | <!-- e.g. pnpm typecheck --> |
| Format | <!-- e.g. pnpm format --> |
| Security scan | <!-- e.g. pnpm audit, semgrep, pip-audit --> |

## Notes
<!-- env setup, required env var NAMES only (never values), CI specifics, gotchas -->
